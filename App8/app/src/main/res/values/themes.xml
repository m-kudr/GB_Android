<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EasyQuiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/ic_launcher_quiz_background</item>
        <item name="font">@font/opensans_regular</item>
    </style>

    <style name="Button">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily">@font/opensans_bold</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="Theme.EasyQuiz" parent="Base.Theme.EasyQuiz" />
</resources>